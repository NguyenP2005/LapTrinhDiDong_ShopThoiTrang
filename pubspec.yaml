name: clothing_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  sqflite: ^2.4.2
  path: ^1.8.3
  provider: ^6.1.1
  shared_preferences: ^2.5.5
  image_picker: ^1.2.1

  # --- THÊM 2 THƯ VIỆN LÀM UI XỊN ---
  video_player: ^2.8.6
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/